enum LoginState {
  unloged,
  carregando,
  logado,
  error,
}

enum SearchState {
  inicial,
  carregando,
  vazio,
  sucesso,
  error,
}

enum AnimeState {
  inicial,
  carregando,
  carregado,
  error,
}
