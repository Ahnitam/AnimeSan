enum LoginState {
  unloged,
  carregando,
  logado,
  error,
}

enum SearchState {
  inicial,
  carregando,
  vazio,
  sucesso,
  error,
}

enum MidiaState {
  inicial,
  carregando,
  carregado,
  error,
}

enum ExternalIdState {
  inicial,
  carregando,
  carregado,
}
